services:
  rabbitmq:
    extends:
      file: rabbitmq-server/docker-compose-rabbitmq.yml
      service: rabbitmq
  redis:
    extends:
      file: redis-server/docker-compose-redis.yml
      service: redis
  postgres_db:
    extends:
      file: postgresql-server/docker-compose-postgres.yml
      service: postgres_db
volumes:
  redis_data:
  postgres_data:
networks:
  microservices_network:
    external: true
